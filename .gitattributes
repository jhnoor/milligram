# LF everywhere, on every OS. Raw string literals keep the line endings of the file they are in, so
# a CRLF checkout on Windows would change the agent's briefing, the launch scripts, and test expectations.
* text=auto eol=lf
